<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.chris.gisamiapp">

    <uses-permission android:name="android.permission.INTERNET" />

    <!--
         The ACCESS_COARSE/FINE_LOCATION permissions are not required to use
         Google Maps Android API v2, but you must specify either coarse or fine
         location permissions for the 'MyLocation' functionality. 
    -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme">
        <activity
            android:name=".com.example.chris.gisamiapp.tematicas.SplashActivity"
            android:theme="@android:style/Theme.Black.NoTitleBar.Fullscreen">

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

        </activity>
        <activity
            android:name=".com.example.chris.gisamiapp.tematicas.TemAnticonActivity"
            android:label="@string/title_activity_tem_ets"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".com.example.chris.gisamiapp.tematicas.TemEtsActivity"
            android:label="@string/title_activity_tem_ets"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".com.example.chris.gisamiapp.tematicas.TemCuidcuerpActivity"
            android:label="@string/title_activity_tem_ets"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".com.example.chris.gisamiapp.tematicas.TemEmbarzActivity"
            android:label="@string/title_activity_tem_ets"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".com.example.chris.gisamiapp.tematicas.TemSaludActivity"
            android:label="@string/title_activity_tem_ets"
            android:theme="@style/AppTheme.NoActionBar" />
        <!--
             The API key for Google Maps-based APIs is defined as a string resource.
             (See the file "res/values/google_maps_api.xml").
             Note that the API key is linked to the encryption key used to sign the APK.
             You need a different API key for each encryption key, including the release key that is used to
             sign the APK for publishing.
             You can define the keys for the debug and release targets in src/debug/ and src/release/. 
        -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="AIzaSyCcM_SZWQngUaWEFiJK4ooFR-PlH3xReYM" />

        <activity
            android:name=".MapsActivity"
            android:label="@string/title_activity_maps" />
        <activity
            android:name=".IntroduccionActivity"
            android:label="Introducción"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".InformActivity"
            android:label="@string/title_activity_inform"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".MenuActivity"
            android:label="@string/title_activity_inform"
            android:theme="@style/AppTheme.NoActionBar" />

        <activity
            android:name=".activity_register"
            android:label="@string/app_name"
            android:launchMode="singleTop"
            android:windowSoftInputMode="adjustPan" />


        <activity
            android:name=".activity_home"
            android:label="@string/title_activity_home"
            android:theme="@style/AppTheme.NoActionBar"></activity>

        <activity android:name="activity.LoginActivity"
            android:label="@string/title_activity_login"
            android:theme="@style/AppTheme.NoActionBar"></activity>

        <activity android:name="activity.RegisterActivity"
            android:label="@string/title_activity_register"
            android:theme="@style/AppTheme.NoActionBar"></activity>

        <activity android:name="activity.MainActivity">
        </activity>

    </application>

</manifest>